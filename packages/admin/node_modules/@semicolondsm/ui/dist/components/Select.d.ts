/// <reference types="react" />
export interface DropDownProps<T> {
    items: ReadonlyArray<T>;
    value?: Readonly<T>;
    overflowOptionDirection?: DirectionList;
    placeholder: string;
    onChange?: (value: T) => void;
}
declare type DirectionList = 'right' | 'left' | 'center';
export declare const Select: <T extends string>({ items, value, overflowOptionDirection, onChange, placeholder, }: DropDownProps<T>) => JSX.Element;
export {};
