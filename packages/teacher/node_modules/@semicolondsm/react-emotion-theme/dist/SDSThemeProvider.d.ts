import React from 'react';
import { ColorScheme, FontTheme } from '@semicolondsm/design-token';
interface Props {
    children: React.ReactNode;
    mode?: 'auto' | 'dark-only' | 'light-only';
}
export declare type SDSTheme = {
    colors: ColorScheme;
    fonts: FontTheme;
};
declare module '@emotion/react' {
    interface Theme extends SDSTheme {
    }
}
export declare const SDSThemeProvider: React.FC<Props>;
export {};
